'use client'
import { useState } from 'react'
import { waLink } from '@/lib/constants'
export default function ContactForm(){ const [f,setF]=useState({name:'',phone:'',email:'',service:'Consultation',message:''}); async function submit(e){e.preventDefault(); await fetch('/api/contacts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)}); window.open(waLink(`Consultation Query\nName: ${f.name}\nPhone: ${f.phone}\nEmail: ${f.email}\nService: ${f.service}\nMessage: ${f.message}`),'_blank')} return <form onSubmit={submit} className="card p-7"><h2 className="font-display text-3xl gold-text">Consultation Form</h2><div className="mt-5 grid gap-4"><input required className="input" placeholder="Full Name" onChange={e=>setF({...f,name:e.target.value})}/><input required className="input" placeholder="Phone Number" onChange={e=>setF({...f,phone:e.target.value})}/><input className="input" placeholder="Email" onChange={e=>setF({...f,email:e.target.value})}/><select className="input" onChange={e=>setF({...f,service:e.target.value})}><option>Consultation</option><option>Legal Drafting</option><option>Litigation Support</option><option>Student Course</option><option>Internship</option></select><textarea className="input min-h-32" placeholder="Message" onChange={e=>setF({...f,message:e.target.value})}/><button className="btn-gold">Submit Query & WhatsApp</button></div></form> }
