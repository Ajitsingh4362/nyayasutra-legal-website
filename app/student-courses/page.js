import Link from 'next/link'; import PageHero from '@/components/PageHero'; import { starterCourses } from '@/lib/constants'; import CTA from '@/components/CTA'
async function getCourses(){ try{ const res=await fetch(`${process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'}/api/courses`,{cache:'no-store'}); const data=await res.json(); return data.courses||starterCourses }catch{return starterCourses}}
export default async function Courses(){ const courses=await getCourses(); return <main><PageHero title="Student Courses & Exam Preparation" text="Learn, Practice, Revise, and Prepare with Confidence through video learning, PDF notes, audio revision, tests and mentorship."/><section className="section"><div className="container grid gap-6 md:grid-cols-2 lg:grid-cols-3">{courses.map(c=><Link href={`/student-courses/${c.slug}`} key={c.slug} className="card p-6 hover:border-gold-400"><h3 className="font-display text-3xl gold-text">{c.title}</h3><p className="mt-3 text-sm leading-7 text-ivory/75">{c.description}</p><div className="mt-5 space-y-2">{(c.modules||[]).slice(0,4).map((m,i)=><div key={i} className="rounded-xl bg-white/5 px-3 py-2 text-sm">{typeof m==='string'?m:m.title}</div>)}</div><span className="btn-outline mt-5">View Course</span></Link>)}</div></section><CTA title="Student Learning Support" text="Courses, internship, drafting, research, bare act notes and mentorship support."/></main> }
