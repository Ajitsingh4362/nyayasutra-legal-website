import PageHero from '@/components/PageHero'
async function getGallery(){ try{ const res=await fetch(`${process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'}/api/gallery`,{cache:'no-store'}); const data=await res.json(); return data.gallery||[] }catch{return []}}
export default async function Gallery(){ const gallery=await getGallery(); return <main><PageHero title="Gallery & Media" text="Court visits, professional interactions, seminars, internship sessions, legal awareness recordings and institutional engagements."/><section className="section"><div className="container grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{gallery.map((m,i)=><div key={i} className="card overflow-hidden">{m.type==='image'?<img src={m.url} alt={m.title} className={`${m.size==='small'?'h-44':m.size==='large'?'h-80':'h-60'} w-full object-cover`}/>:<div className="grid h-60 place-items-center bg-white/5 p-5 text-center"><a className="btn-outline" href={m.url} target="_blank">Open {m.type}</a></div>}<div className="p-5"><p className="text-sm gold-text">{m.category}</p><h2 className="mt-2 font-display text-2xl">{m.title}</h2><p className="mt-2 text-sm text-ivory/70">{m.description}</p></div></div>)}</div></section></main> }
