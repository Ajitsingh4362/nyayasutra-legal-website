'use client'
import { useState } from 'react'
import { waLink } from '@/lib/constants'
export default function InternshipForm(){ const [f,setF]=useState({name:'',phone:'',email:'',courseTitle:'Internship Application',message:''}); async function submit(e){e.preventDefault(); await fetch('/api/enrollments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f, courseSlug:'internship'})}); window.open(waLink(`Internship Application%0AName: ${f.name}%0APhone: ${f.phone}%0AEmail: ${f.email}%0AMessage: ${f.message}`),'_blank')} return <form onSubmit={submit} className="card p-7"><h2 className="font-display text-3xl gold-text">Internship Application</h2><div className="mt-5 grid gap-4"><input required className="input" placeholder="Full Name" onChange={e=>setF({...f,name:e.target.value})}/><input required className="input" placeholder="Phone Number" onChange={e=>setF({...f,phone:e.target.value})}/><input className="input" placeholder="Email" onChange={e=>setF({...f,email:e.target.value})}/><textarea className="input min-h-28" placeholder="Your interest / college / experience" onChange={e=>setF({...f,message:e.target.value})}/><button className="btn-gold">Submit & Continue on WhatsApp</button></div></form> }
