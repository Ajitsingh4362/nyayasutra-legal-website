import Link from 'next/link'; import PageHero from '@/components/PageHero'; import { starterBlogs } from '@/lib/constants'
async function getBlogs(){ try{ const res=await fetch(`${process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'}/api/blogs`,{cache:'no-store'}); const data=await res.json(); return data.blogs||starterBlogs }catch{return starterBlogs}}
export default async function Blog(){ const blogs=await getBlogs(); return <main><PageHero title="Legal Intelligence & Articles" text="Constitutional law, criminal law analysis, Supreme Court judgments, high court updates, arbitration, corporate law, cyber law and legal current affairs."/><section className="section"><div className="container grid gap-6 md:grid-cols-3">{blogs.map(b=><Link href={`/blog/${b.slug}`} key={b.slug} className="card overflow-hidden hover:border-gold-400">{b.featuredImage&&<img src={b.featuredImage} alt={b.title} className="h-52 w-full object-cover"/>}<div className="p-6"><p className="text-sm gold-text">{b.category}</p><h2 className="mt-2 font-display text-3xl">{b.title}</h2><p className="mt-3 text-sm text-ivory/70">{b.excerpt}</p><span className="mt-5 inline-block text-gold-300">Read More →</span></div></Link>)}</div></section></main> }
